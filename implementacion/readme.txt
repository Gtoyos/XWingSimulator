Para ejecutar el cliente:
python3 cliente.py

Para ejecutar el servidor:
python3 servidor.py

En caso de que no se tengan las librerias que utiliza el cliente o el servidor, es posible instalarlas con el comando:
pip3 install -r requirements.txt
